# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - nft-admintool

on:
  push:
    branches:
      - main
    paths:
      - 'nft-admin-tool/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: false

    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'          

      - name: npm install, build, and test
        working-directory: nft-admin-tool/nft-admin-tool
        run: |
         npm install
         npm run build --if-present
        # npm run test --if-present
   
      - name: Zip artifact 
        working-directory: nft-admintool
        run: zip -r nft-admintool.zip ./*
        
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v3
        with:
          name: nft-admintool
          path: nft-admin-tool/nft-admintool.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: nft-admintool
          
      - name: Unzip artifact
        run: unzip nft-admintool.zip
          
      - name: Remove zip
        run: rm nft-admintool.zip

      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'nft-admintool'
          slot-name: 'Production'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_019A2E132C8743B98437A6A6036C5167 }}
          package: .
